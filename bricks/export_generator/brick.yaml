name: export_generator
description: Regenerates per-feature export barrels ({feature}_exports.dart). Leave feature_name empty to update all features.
version: 0.1.0

vars:
  feature_name:
    type: string
    description: Feature to regenerate exports for (leave empty to update all features)
    default: ""
    prompt: "Feature name to update (leave empty for all features):"
